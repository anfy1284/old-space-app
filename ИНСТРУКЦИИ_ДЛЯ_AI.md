# ИНСТРУКЦИИ ДЛЯ AI АССИСТЕНТА

## КАК РАБОТАТЬ С ЭТИМИ ФАЙЛАМИ

1. **В НАЧАЛЕ работы над задачей:** Пользователь скажет "сначала прочти инструкции"
   - Прочитай этот файл полностью
  - Прочитай `АРХИТЕКТУРА_ПРОЕКТА.md` для технических деталей (автоматически — см. правило ниже)
   - Примени эти принципы при решении задачи

2. **В КОНЦЕ работы над задачей:** Пользователь скажет "дополни инструкции"
   - Проанализируй чат и найди свои ошибки
   - Добавь новые принципы в этот файл
   - Добавь технические детали в `АРХИТЕКТУРА_ПРОЕКТА.md`

---

## ОСНОВНЫЕ ПРИНЦИПЫ РАБОТЫ

### 1. СИСТЕМНЫЙ ПОДХОД ВМЕСТО КОСТЫЛЕЙ

❌ **НЕПРАВИЛЬНО:**
- Искать объекты через глобальные реестры (window._something)
- Использовать setTimeout для ожидания инициализации
- Перехватывать/заменять методы объектов
- Искать элементы по индексам (array[0], первый элемент)
- Делать сложные обходные решения

✅ **ПРАВИЛЬНО:**
- Встраивать функциональность в базовые классы
- Использовать стандартные механизмы (callbacks, properties, events)
- Создавать прямые связи между объектами при их создании
- Делать автоматические настройки на уровне архитектуры

**Пример из нашего чата:**
- Неправильно: Искать DynamicTable в window._dynamicTableSubscribers
- Правильно: DataForm автоматически создает связь с таблицей при рендеринге

### 2. СЛУШАЙ ПОЛЬЗОВАТЕЛЯ

Если пользователь говорит:
- "это не то" / "не помогло" / "не правильно" - **остановись и переосмысли подход**
- "нужно исправлять внутри приложения X" - исправляй ТОЛЬКО там
- "это костыль" / "ненадежно" - ищи системное решение
- "почему ты..." - объясни логику, возможно она неверная

**НЕ ПЫТАЙСЯ защищать неправильное решение. Признай ошибку и ищи другой подход.**

### 3. ДУМАЙ ШИРОКО, НАЧИНАЙ С АРХИТЕКТУРЫ

Перед тем как писать код:
1. Где ДОЛЖНА быть эта функциональность по архитектуре?
2. Есть ли уже механизм для этого? (callbacks, events, properties)
3. Можно ли сделать это универсально для всех случаев?
4. Нужно ли менять базовые классы вместо хаков в приложении?

**Порядок решения:**
1. Анализ: Где проблема? В каком слое? (UI, данные, связи)
2. Архитектура: Как должно работать правильно?
3. Реализация: Минимальное изменение в правильном месте
4. Проверка: Работает для всех случаев?

### 4. УНИВЕРСАЛЬНОСТЬ ВМЕСТО ЧАСТНЫХ СЛУЧАЕВ

❌ **Плохо:** Решение работает только для uniListForm  
✅ **Хорошо:** Решение работает для всех DataForm с DynamicTable

❌ **Плохо:** Код знает про конкретную структуру layout  
✅ **Хорошо:** Код работает с любым layout

### 5. ПРЯМЫЕ СВЯЗИ ВМЕСТО ПОИСКА

❌ **Неправильно:**
```javascript
// Искать таблицу после создания
setTimeout(() => {
    const table = findTableSomehow();
});
```

✅ **Правильно:**
```javascript
// Создать связь сразу при создании
const table = new DynamicTable(config);
form.table = table;
table.onRowActivate = (row) => form._currentRecord = row;
```

### 6. МИНИМАЛЬНОЕ ЛОГИРОВАНИЕ В ПРОДАКШН КОДЕ

Во время отладки - логируй все.  
После исправления - удали большую часть логов.  
Оставь только критичные ошибки.

### 7. ЧИТАЙ КОД БАЗОВЫХ КЛАССОВ

Перед тем как делать костыль:
- Посмотри на DataForm - может там уже есть механизм?
- Посмотри на DynamicTable - какие у него есть свойства/методы?
- Посмотри как другие компоненты решают похожие задачи

### 8. CSS ДЛЯ LAYOUT, JS ДЛЯ ЛОГИКИ

❌ **НЕПРАВИЛЬНО:**
- Устанавливать flex/display/позиционирование через JS
- Манипулировать стилями layout в коде компонентов
- Дублировать CSS правила в JS

✅ **ПРАВИЛЬНО:**
- Проверь наличие style.css ПЕРЕД изменением JS
- Layout через CSS классы и правила
- JS только для динамической логики (не для статических стилей)
- Используй CSS селекторы для управления видом вложенных элементов

**Пример из нашего чата:**
- Неправильно: Добавлять `element.style.flex = '1'` в renderItem для каждого элемента
- Правильно: В CSS: `.ui-form-content { display: flex; flex-direction: column; }` и `.ui-dynamictable { flex: 1; }`

### 9. ИЩИТЕ СУЩЕСТВУЮЩИЕ МЕХАНИЗМЫ

Прежде чем добавлять новую функциональность:
1. Проверь наличие CSS файлов (style.css)
2. Поищи классы элементов в CSS
3. Проверь есть ли уже похожее решение
4. Только потом добавляй новое

**Не изобретай велосипед - используй то что уже есть.**

### 10. ПРОСТОТА КАК ВРЕМЕННОЕ РЕШЕНИЕ

**Золотое правило:** Если сложное решение правильное по архитектуре/дизайну, но не получается - сделай простое как ВРЕМЕННОЕ.

❌ **НЕПРАВИЛЬНО:**
- Говорить "сложное решение плохое" когда оно правильное
- Защищать простое решение когда оно не соответствует дизайну
- Отказываться от правильного решения из-за своих ошибок в реализации

✅ **ПРАВИЛЬНО:**
- Признать что не справился со сложным решением
- Сделать простое как ВРЕМЕННОЕ решение
- Оставить заметку что нужно вернуться к сложному позже
- Не путать "простое" с "правильным"

**Пример из нашего чата (выравнивание заголовков таблицы):**
1. ❌ Попытка 1: Динамический padding с таймаутами - не работает (проблема с async)
2. ✅ Попытка 2: Кастомный Win95 scrollbar на canvas - ПРАВИЛЬНОЕ решение для ретро-стиля
   - Но AI не справился: не работало колесико мыши в первой реализации
   - Попытка со скрытым нативным скроллом тоже не заработала
3. 🔧 **ВРЕМЕННОЕ РЕШЕНИЕ:** `overflow-y: scroll` + `padding-right: (scrollbarWidth - 1)px`
   - Это РАБОТАЕТ, но не соответствует Win95 дизайну
   - Нужно вернуться к кастомному scrollbar позже

**Когда простое решение НЕПРАВИЛЬНОЕ:**
- Когда оно нарушает дизайн/стиль проекта (нативный scrollbar в Win95 интерфейсе)
- Когда пользователь явно хотел другое решение
- Когда "простое" - это костыль

**Процесс выбора:**
1. Попробуй правильное решение по архитектуре/дизайну
2. Если не получается - попробуй другую реализацию
3. Если всё равно не получается - ПРИЗНАЙ ЧТО НЕ СПРАВИЛСЯ
4. Сделай временное простое решение
5. Оставь TODO для возврата к правильному решению

**Важно:** Не защищай свою ошибку, говоря что "простое всегда лучше". Простое - это компромисс, когда сложное не получилось реализовать.

---

## ANTI-PATTERNS ИЗ НАШЕГО ЧАТА

### ❌ Множественные итерации с одинаковым подходом
Если решение не работает 2 раза - подход неверный. Меняй стратегию.

### ❌ Перехват методов для побочных эффектов
```javascript
// ПЛОХО
obj.method = function() {
    original.call(this);
    doSomethingElse();
}
```

### ❌ Глобальные реестры для локальных связей
Не используй window.something для связи между form и table.

### ❌ Таймауты для синхронизации
setTimeout - признак плохой архитектуры.

### ❌ Поиск объектов по индексам/позициям
layout[0], controlsMap.keys()[0] - ненадежно.

### ❌ Изменение layout через JS вместо CSS
Не устанавливай display, flex, позиционирование через element.style - используй CSS.

### ❌ Игнорирование существующих файлов
Всегда проверяй наличие style.css, config файлов, существующих решений.

---

## СТИЛИЗАЦИЯ UI КОМПОНЕНТОВ

### 11. ВСЕ СТИЛИ В CSS, НЕ В JS

❌ **НЕПРАВИЛЬНО:**
```javascript
// Inline стили для каждого элемента
element.style.width = '22px';
element.style.height = '22px';
element.style.border = '2px solid #808080';
```

✅ **ПРАВИЛЬНО:**
```css
/* В style.css */
.input-field-button {
  width: 22px;
  height: 22px;
  border: 2px solid #808080;
}
```

```javascript
// В JS только добавляем класс
element.classList.add('input-field-button');
```

### 12. АДАПТИВНЫЕ РАЗМЕРЫ ЧЕРЕЗ CSS

**Используй относительные единицы и CSS свойства вместо фиксированных пикселей:**

❌ **Плохо:** Фиксированный размер в пикселях
```css
.button { width: 22px; height: 22px; }
```

✅ **Хорошо:** Размер относительно контейнера
```css
.input-field-button {
  align-self: stretch;  /* растягивается по высоте контейнера */
  height: 100%;
  aspect-ratio: 1 / 1;  /* ширина = высоте */
  width: auto;
}
```

### 13. КОГДА CSS НЕДОСТАТОЧНО - МИНИМАЛЬНЫЙ JS

Иногда CSS не может решить задачу (например, `aspect-ratio` не работает с `height:100%` в некоторых браузерах). Тогда:

✅ **Правильный подход:**
1. Максимум стилей в CSS (цвет, borders, padding, font)
2. Минимум JS для того что CSS не может (синхронизация width = computedHeight)
3. Правильная очистка observers/listeners

```javascript
// Только синхронизация размера
const syncBtn = (b) => {
    const update = () => {
        const h = Math.round(b.offsetHeight);
        if (h > 0) b.style.width = h + 'px';
    };
    update();
    if (typeof ResizeObserver !== 'undefined') {
        const ro = new ResizeObserver(update);
        ro.observe(b.parentElement || b);
        b._ro = ro;  // Сохранить для cleanup
    - **Останавливайся при сомнении**: если решение может нарушить архитектуру — остановись и уточни у пользователя.

    ---

    ## ДОПОЛНИТЕЛЬНЫЕ ПРОМПТЫ И ШАБЛОНЫ ДЛЯ AI (ПОЭТАПНАЯ РАБОТА)
    Чтобы в следующий раз избежать недоразумений и неинформативных действий, добавляем конкретные шаблоны и пред- / пост-действия, которые AI должен автоматически применять.

    - **Перед любыми правками (автоматический пред-прамбл):**
      - Показ: 1 предложение — что собираюсь сделать.
      - Шаблон: "Я собираюсь внести минимальное изменение в [ФАЙЛ] для [ЦЕЛЬ]. Продолжать?"

    - **Чеклист перед правкой (универсальный, применить автоматически):**
      - Проверить наличие связанного CSS/стилей перед изменением визуальной логики.
      - Проверить, есть ли в коде переменные, параметры или хелперы (например, `openParams`, `appForm`, `instance`) которые уже решают задачу — использовать их в первую очередь.
      - Искать существующие методы/колбэки (например, `onSelect`, `onOpen`, `onRowActivate`) и вызывать их вместо добавления новых точек интеграции.
      - Минимизировать изменения: править точечно, не рефакторить лишнее.
      - Не менять `node_modules` без явного подтверждения пользователя; если изменение всё же нужно — подготовить патч и запросить подтверждение.

    - **Патч/коммитный шаблон (короткий):**
      - "[AREA] Fix: [короткое описание] — minimal change; reason: [почему]."

    - **Запрещённые предложения (не предлагать без явной просьбы):**
      - Не предлагай запуск тестов и не запускай их без просьбы пользователя.
      - Не советуй перезагрузку страницы/сервер (Ctrl+F5) — пользователь это делает сам.

    - **Шаблоны вызовов/параметров:**
      - Если в коде есть `openParams`, `params` или `instance` — используй их (например, `appForm.selectMode = openParams.selectMode || false`).
      - Для вызовов `onSelect` передавать пустой объект `{}` по умолчанию; если нужно — можно добавить `{ record: row }` по запросу пользователя.

    - **Пост-правка (короткий отчёт):**
      - После успешной правки отправлять 2 предложения: что изменил (файл + строка/область) и краткая причина. Пример: "Изменил [путь/файл]: добавил assign selectMode; цель: обеспечить поведение выбора при открытии формы." Если изменения затронули `node_modules`, явно подчеркнуть это и попросить подтверждение на сохранение.

    Эти пункты — рекомендация, которую AI должен применять автоматически перед правкой и при коммуникации с пользователем, чтобы исключить повторение ошибок из текущей переписки.
    }
};
```

**Не забывай cleanup:**
```javascript
destroy() {
    if (this._selectBtn?._ro) this._selectBtn._ro.disconnect();
    if (this._selectBtn?._win) window.removeEventListener('resize', this._selectBtn._win);
    // ... остальная очистка
}
```

### 14. УНИФИКАЦИЯ СТИЛЕЙ

Если есть несколько похожих элементов (кнопки "..." и "▾"):
- НЕ создавай отдельные классы `.button-small`, `.button-large`
- Используй ОДИН класс `.input-field-button` для всех
- Размер определяется контекстом (высотой контейнера), а не типом кнопки

❌ **Плохо:**
```css
.input-field-button { width: 22px; height: 22px; }
.input-field-button.small { width: 18px; height: 18px; }
```

✅ **Хорошо:**
```css
.input-field-button { 
  /* Один размер, адаптируется к контейнеру */
  height: 100%;
  aspect-ratio: 1/1;
}
```

---

## ЧЕКЛИСТ ПЕРЕД ОТПРАВКОЙ РЕШЕНИЯ

1. ✅ Это системное решение или костыль?
2. ✅ Это работает универсально или только для текущего случая?
3. ✅ Код находится в правильном месте архитектуры?
4. ✅ Я использовал стандартные механизмы (не изобретал свои)?
5. ✅ Нужен ли cleanup или объекты умрут сами?
6. ✅ Я проверил базовые классы на наличие готовых решений?
7. ✅ Я проверил наличие CSS файлов и существующих стилей?
8. ✅ Layout делается через CSS, а не через JS?
9. ✅ Стили вынесены в CSS, inline стили минимальны?
10. ✅ Размеры адаптивные (относительные), а не фиксированные в пикселях?
11. ✅ Добавлен cleanup для observers/listeners если используется JS для размеров?

---

## СПЕЦИФИЧНО ДЛЯ ЭТОГО ПРОЕКТА

См. файл `АРХИТЕКТУРА_ПРОЕКТА.md` для технических деталей:
- Структура MySpace Framework
- Как работает DataForm
- Как работает DynamicTable
- Паттерны создания и уничтожения объектов

## ПРАВИЛА ДЛЯ AI-АССИСТЕНТА (КОРОТКО)
- **Обязательное прочтение (автоматически):** AI обязан автоматически прочитывать этот файл перед началом любой задачи и действовать согласно ему; нельзя задавать пользователю вопрос о необходимости чтения — просто читай и выполняй правила.
- **Чтение архитектуры (строго обязательно):** AI обязан автоматически прочитывать `АРХИТЕКТУРА_ПРОЕКТА.md` вместе с этим файлом перед началом любой задачи и действовать согласно их положениям; категорически запрещено спрашивать пользователя "Хотите, чтобы я прочитал ARХИТЕКТУРА_ПРОЕКТА.md?" или аналогичные вопросы.
- **Сначала прочти**: всегда открывай и читай этот файл перед началом работы.
- **Согласуй изменения в node_modules**: не вноси инвазивные правки в `node_modules` без явного разрешения пользователя.
- **Минимализм изменений**: предпочитай простые, CSS-решения для UI и минимальные правки в коде.
- **Поясняй коротко**: перед крупными правками отправь 1-2 коротких предложения о том, что собираешься сделать.
- **Тестируй локально**: запускай доступные скрипты/тесты после правок и сообщай результат.
- **Документируй правки**: коротко опиши в ответе какие файлы изменил и почему.
- **Останавливайся при сомнении**: если решение может нарушить архитектуру — остановись и уточни у пользователя.
